<%= form_with(model: api_language, local: true) do |form| %>
  <% if api_language.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_language.errors.count, "error") %> prohibited this api_language from being saved:</h2>

      <ul>
      <% api_language.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
